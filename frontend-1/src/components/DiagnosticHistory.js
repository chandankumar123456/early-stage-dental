import React from 'react';
import './DiagnosticHistory.css';

/**
 * DiagnosticHistory is intentionally empty.
 * It will render content only when a backend endpoint for
 * patient history exists. Currently no such API endpoint exists.
 */
export default function DiagnosticHistory() {
  return null;
}
